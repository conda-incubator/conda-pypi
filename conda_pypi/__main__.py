# This module previously provided a standalone CLI interface
# The main functionality is now available via the conda plugin: `conda pip`
print("conda-pypi is now available as a conda plugin. Use: conda pip --help")
