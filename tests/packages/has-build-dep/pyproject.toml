[build-system]
# packaging has no dependencies in recent versions and is available on conda-forge
requires = ["flit_core", "packaging"]

[project]
name = "has_dep"
version = "1"
description = "has dependency unlikely to exist in test environment"
